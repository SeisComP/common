SET(
	ROUTER_LOCATOR_SOURCES
		router.cpp
)

SC_ADD_PLUGIN_LIBRARY(ROUTER_LOCATOR locrouter "")

SC_LINK_LIBRARIES_INTERNAL(locrouter core)

FILE(GLOB descs "${CMAKE_CURRENT_SOURCE_DIR}/descriptions/*.xml")
INSTALL(FILES ${descs} DESTINATION ${SC3_PACKAGE_APP_DESC_DIR})
