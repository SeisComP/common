SET(
	LOCATOR_EXTERNAL_SOURCES
		external.cpp
)

SC_ADD_PLUGIN_LIBRARY(LOCATOR_EXTERNAL locext "")

SC_LINK_LIBRARIES_INTERNAL(locext core)

FILE(GLOB descs "${CMAKE_CURRENT_SOURCE_DIR}/descriptions/*.xml")
INSTALL(FILES ${descs} DESTINATION ${SC3_PACKAGE_APP_DESC_DIR})